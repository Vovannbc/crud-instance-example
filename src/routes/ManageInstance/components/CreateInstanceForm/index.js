import React from "react";
import PropTypes from "prop-types";

const CreateInstanceForm = props => {
    return (
        <div>

        </div>
    );
};

CreateInstanceForm.propTypes = {

};

export default CreateInstanceForm;
